import React from "react";
import { Icon } from "../../layout/icon/Icon"

export const Logo:React.FC = () => {
    return (
        <a href=''>
            <Icon iconId={'code'} />
        </a>
    );
};